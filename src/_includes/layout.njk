<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      {{ renderData.title or title or metadata.title }} | TGIFelix
    </title>
    <meta name="Description" content="" />
    {% set css %}{% include "style.css" %}{% endset %}
    <style>
      {{ css | cssmin | safe }}
    </style>
  </head>

  <body>
    <header>
      <h1>{{ metadata.title }}</h1>
      <div id="nav">
        {%- for nav in collections.nav -%}
        <a href="{{ nav.url | url }}">{{ nav.data.navtitle }}</a><br />
        {%- endfor -%}
      </div>
    </header>
    <main>
      {{ content | safe }}
    </main>
  </body>
  
  {% set js %}{% include "script.js" %}{% endset %}
  <script>
    {{ js | jsmin | safe }}
  </script>

</html>